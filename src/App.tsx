import { useLocation } from 'react-router-dom';
import Router from './routes/Router';

const App = () => {
  return (
    <div className="app-container">
      <Router />
    </div>
  );
};

export default App;
