import styled from 'styled-components';

const Home = () => {
  return <Title>KB스타뱅킹</Title>;
};

const Title = styled.h1`
  color: var(--g10);
  font-size: 24px;
  font-weight: bold;
`;

export default Home;
